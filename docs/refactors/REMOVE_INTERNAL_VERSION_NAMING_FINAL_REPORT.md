# Remove Internal Version Naming - Final Report

## Verdict

COMPLETED.

The active product no longer uses internal version naming as a runtime concept, API discriminator, configuration alias, source filename, active documentation label, frontend constant, or persisted detailed-match discriminator.

Historical references are intentionally limited to archived documentation under `docs/archive/`, which is outside the active product surface.

## 1. Initial reference inventory

The cleanup audited the root repository and the frontend repository for:

- `legacy token A`
- `legacy token B`
- `legacy token A`
- `legacy token B`
- `legacy detail data-source token`
- `use-legacy token A-engine`
- `use-legacy token B-detailed-engine`
- `app.simulation.legacy token B`
- `legacy engine version field`

Initial references appeared in backend simulation naming, detailed match persistence, configuration properties, tests, active reports, frontend detailed match models, frontend match-detail tests, player stat data-source constants, scripts, and active documentation.

## 2. Names eliminated

Removed from active code/config/docs:

- `Persistedlegacy engine version fields`
- `legacy engine version field`
- `DETAILED_MATCH_ENGINE_VERSION`
- `PLAYER_STATS_DETAILED_DATA_SOURCE`
- `legacy detail data-source token`
- `use-legacy token A-engine`
- `use-legacy token B-detailed-engine`
- `app.simulation.legacy token B.*`
- versioned active filenames and report names containing internal version

Functional replacements now describe behavior instead of internal development phases:

- `DetailedMatchDiscriminators`
- `engineType`
- `DETAILED_MATCH`
- `DETAILED_PLAYER_STATS`
- `app.simulation.league.use-classic-engine`
- `app.simulation.league.detailed-enabled`
- `app.simulation.detailed.*`
- `app.simulation.career-mutations.*`

## 3. Models and fields renamed

Backend detailed match persistence now stores the detailed engine discriminator as:

- field: `engineType`
- value: `DETAILED_MATCH`

Frontend detailed match models now consume the same functional field/value:

- `engineType`
- `DETAILED_MATCH`
- `DETAILED_PLAYER_STATS`

The old `legacy engine version field` naming was removed from active backend and frontend contracts.

## 4. Configuration removed

Removed deprecated/internal version properties:

- `app.simulation.league.use-legacy token A-engine`
- `app.simulation.league.use-legacy token B-detailed-engine`
- `app.simulation.legacy token B.*`

Remaining active configuration uses functional names:

- `app.simulation.league.use-classic-engine`
- `app.simulation.league.detailed-enabled`
- `app.simulation.detailed.*`
- `app.simulation.career-mutations.*`

## 5. Data migrated or cleaned

No public user data compatibility layer was preserved because this is still a pre-public development environment.

Detailed match payloads generated by the runtime smoke now persist `engineType = DETAILED_MATCH` and do not leak internal version or `legacy detail data-source token`.

Local PostgreSQL validation exposed invalid, non-runnable migration filenames. They were replaced with valid Flyway migrations:

- `V13__add_player_height_skills.sql`
- `V14__add_division_to_teams.sql`
- `V15__add_league_id_to_teams.sql`
- `V16__per_league_division.sql`

The local Flyway history was repaired in the development database so the runtime could apply the current migration chain and validate the app end to end.

## 6. Frontend changes

Updated the detailed match frontend surface:

- removed `detailed-match-compatibility.model.ts`
- added `detailed-match-discriminators.model.ts`
- replaced versioned constants with functional detailed-match discriminators
- updated match-detail models, tests, compare tests, API service tests, and player-season-stats model imports
- cleaned active frontend documentation references

Frontend active scan result:

- zero internal version references in source, tests, active docs, filenames, and config, excluding `node_modules` and `dist`

## 7. Backend changes

Updated backend simulation and persistence:

- replaced `Persistedlegacy engine version fields` with `DetailedMatchDiscriminators`
- renamed `legacy engine version field` to `engineType`
- removed internal version configuration aliases
- updated detailed simulation configuration
- updated integration/unit tests and active documentation
- renamed active internal version report/test files to functional names
- fixed local Flyway migration naming so the app can start and seed data correctly

Backend active scan result:

- zero internal version references in source, tests, active docs, resources, scripts, and filenames, excluding generated output and `docs/archive/`

## 8. Remaining references

Allowed remaining references:

- historical archived documentation under `docs/archive/`

No active product references remain for:

- `legacy token A`
- `legacy token B`
- `legacy token A`
- `legacy token B`
- `legacy detail data-source token`
- `use-legacy token A-engine`
- `use-legacy token B-detailed-engine`
- `app.simulation.legacy token B`
- `legacy engine version field`

## 9. Validation

Backend:

- `mvn -q -DskipTests test-compile`: passed
- `mvn -q test`: passed
- Surefire result: 3915 tests, 0 failures, 0 errors, 8 skipped

Frontend:

- `npm run build -- --configuration development`: passed
- `npm run build`: passed
- `npm test -- --watch=false --browsers=ChromeHeadless`: passed
- Karma result: 1016 success, 0 failures, 2 skipped

Repository hygiene:

- root active reference scan: clean, excluding generated output and `docs/archive/`
- frontend active reference scan: clean, excluding generated output

## 10. Runtime

Runtime full-stack smoke passed through the frontend proxy and backend:

- register user
- seed La Liga
- fetch leagues and teams
- start career
- auto-select lineup with `4-4-2`
- confirm lineup
- advance round
- fetch fixtures
- start live round
- poll detailed match persistence
- verify persisted detailed match payload uses `engineType = DETAILED_MATCH`
- verify persisted detailed match payload contains no internal version, `legacy detail data-source token`, or `legacy engine version field`

Runtime success marker:

- `VERSIONLESS_RUNTIME_SMOKE_OK`

## 11. Final verdict

COMPLETED.

The current product now uses functional detailed-match naming instead of internal version naming, validates backend/frontend behavior, and passes a full-stack runtime smoke.

