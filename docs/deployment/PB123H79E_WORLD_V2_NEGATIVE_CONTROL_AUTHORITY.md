# World V2 Negative-Control Authority

The canonical set contains 33 unique control IDs and 33 unique mutations.

| Mode | Count |
|---|---:|
| Redis physical | 22 |
| Source-proven | 11 |
| Duplicate credit | 0 |
| Material proxy | 0 |
| False pass | 0 |

Every row records `control_id`, targeted finding, required and actual mode, fixture, mutation, pipeline entry, expected result, actual result, detection, and error code. Redis representation, migration state, catalog, capacity, semantic delta, alias graph, concurrency/source-CAS, and owner isolation findings have physical integration evidence. Source-proven mode is limited to identity algebra, impossible type/shape states, and fail-closed source inventories.

The prior duplicate pairs no longer share credited mutations: foreign envelope ownership differs from requested/source ownership, and corrupt catalog hash differs from a changed durable canonical source. The former alias false-positive is independently reproduced by a physical committed-envelope mutation and rejected by the comparator or repository, depending on graph validity.
