spring:
  config:
    activate:
      on-profile: career-mutations

app:
  simulation:
    detailed:
      mutate-career-state: true
      persist-discipline: true
      persist-injuries: true
      persist-fatigue: false
      persist-form: false
