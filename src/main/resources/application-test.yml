spring:
  config:
    activate:
      on-profile: test

# Test harness deterministic random seed for reproducible E2E tests.
# Default 42 preserves existing test behaviour.
app:
  simulation:
    random-seed: 42